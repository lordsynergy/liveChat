<h3 class='text-center mb-3 mt-5'>Editing user</h3>

<%= render 'users/form' %>

<div class="actions mb-3 text-center">
  <%= button_to 'Cancel my account', @user,
                method: :delete, class: 'btn btn-danger mb-3 mt-1', form: { data: { turbo_confirm: 'Are you sure?' } } %>
</div>
